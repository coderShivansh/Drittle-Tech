import React from 'react';

type Props = {};

/**
 * @param {Props} props
 * @return RSC (React Server Component)
 */
const IntershipLoadingPage = ({}: Props) => {
  return <div>Loading....</div>;
};

export default IntershipLoadingPage;
