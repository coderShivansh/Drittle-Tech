import React from 'react';

type Props = {};

/**
 * @param {Props} props
 * @return RSC (React Server Component)
 */
const RootLoadingPage = ({}: Props) => {
  return <div>Loading....</div>;
};

export default RootLoadingPage;
