import React from 'react';

type Props = {};

/**
 * @param {Props} props
 * @return RSC (React Server Component)
 */
const AboutLoadingPage = ({}: Props) => {
  return <div>Loading....</div>;
};

export default AboutLoadingPage;
